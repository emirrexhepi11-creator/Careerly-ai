function scrollToAssistant(){document.getElementById('assistant').scrollIntoView({behavior:'smooth'});setTimeout(()=>document.getElementById('prompt').focus(),500)}
function usePrompt(text){document.getElementById('prompt').value=text;scrollToAssistant()}
function addMessage(text,cls){const box=document.getElementById('messages');const el=document.createElement('div');el.className='message '+cls;el.textContent=text;box.appendChild(el);box.scrollTop=box.scrollHeight}
function askAI(){const input=document.getElementById('prompt');const text=input.value.trim();if(!text)return;addMessage(text,'user');input.value='';setTimeout(()=>{let reply='Great question. Start by matching your CV to the job description, highlighting relevant skills, and keeping your application clear and specific.';if(/cv|resume/i.test(text))reply='For a stronger CV, use a clear summary, list your strongest relevant skills, add measurable achievements where possible, and tailor keywords to the job description.';if(/cover|application/i.test(text))reply='A good application should be short and tailored: mention the role, explain why your skills fit, give one concrete example, and finish with a confident call to action.';if(/skill/i.test(text))reply='For entry-level support roles, useful skills include written communication, problem solving, patience, basic computer skills, organization and customer-focused thinking.';addMessage(reply,'ai')},500)}